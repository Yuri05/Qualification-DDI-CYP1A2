name: Check Latest Model Releases in a Qualification Plan

on:
  workflow_dispatch:
  pull_request:
  push:
    branches-ignore:
      - master
      - main

jobs:
  check-latest-releases:
    uses: ./.github/workflows/CheckForLatestModelReleasesInQualificationPlan.yml